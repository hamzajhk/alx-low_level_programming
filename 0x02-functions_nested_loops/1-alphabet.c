#include <main.h>
/**
 * main - print alphabet lowercase
 *
 * Return : Always 0 (success)
 */
void print_alphabet(void);
{
	char = alphabet
		for (alphebt = 'a'; alphabet <= 'z'; alphabet++)
			_putchar(alphabet"\n");
}
